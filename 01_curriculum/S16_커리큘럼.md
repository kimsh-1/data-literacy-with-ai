# S16 데이터 모델링: 관계, 스타 스키마, 날짜 차원

## 차시 개요

- 대상: 엑셀은 쓰지만 데이터분석은 처음인 비개발자
- 선수 차시: S15, S04
- 난이도: 4
- 시간: 70분
- 사용 도구: Power BI Desktop
- 사용 데이터: `02_datasets/shop_orders/orders.csv`, `customers.csv`, `products.csv`
- Power BI 실습 파일: `/mnt/d/ai-data-analysis-course/02_datasets/shop_orders/shop_orders_clean.xlsx` (같은 `orders`, `customers`, `products` 데이터)
- Windows 파일 경로: `D:\ai-data-analysis-course\02_datasets\shop_orders\shop_orders_clean.xlsx`

이번 차시는 Power BI에서 여러 표를 한 모델로 연결하는 방법을 다룬다. S04에서는 `VLOOKUP`으로 주문 표에 단가, 지역, 등급을 직접 붙였다. S16에서는 같은 문제를 다른 방식으로 푼다. `orders`, `customers`, `products`를 한 표로 합치지 않고, 각 표의 역할을 유지한 채 관계를 맺는다.

수업의 핵심은 `orders`를 중심에 둔 스타 스키마다. 주문 한 건 한 건은 매출을 만들 수 있는 사건이고, 고객과 상품은 그 사건을 설명하는 정보다. 여기에 날짜 차원을 추가하면 S17에서 전월대비, 누적, 월별 추세 같은 시간 계산을 안정적으로 만들 수 있다.

## 학습 목표

수강생은 이 차시가 끝나면 다음을 할 수 있어야 한다.

1. Power BI에서 1:다 관계를 만들고 관계 방향을 확인할 수 있다.
2. 팩트 테이블과 차원 테이블을 구분할 수 있다.
3. `orders`, `customers`, `products`, `날짜테이블`로 스타 스키마를 구성할 수 있다.
4. 주문일 범위를 기준으로 날짜 차원 테이블을 만들고 날짜 테이블로 지정할 수 있다.
5. 고객 차원의 `region` 필드로 주문 매출을 지역별로 분해할 수 있다.

## 실제 데이터 구조

pandas 3.0.3으로 실제 파일을 확인한 결과는 다음과 같다.

| 표 | 행 수 | 열 수 | 주요 컬럼 | 역할 |
|---|---:|---:|---|---|
| `orders` | 20,000 | 6 | `order_id`, `customer_id`, `product_id`, `order_date`, `qty`, `discount_rate` | 주문 사건이 쌓인 팩트 |
| `customers` | 3,000 | 7 | `customer_id`, `region`, `grade`, `join_date` | 고객을 설명하는 차원 |
| `products` | 200 | 5 | `product_id`, `product_name`, `category`, `price`, `cost` | 상품을 설명하는 차원 |

검증 결과 `customers[customer_id]`와 `products[product_id]`는 각각 고유값이고, `orders`의 고객번호와 상품번호는 모두 양쪽 차원 테이블에 존재한다. 주문일은 2024년 1월 1일부터 2024년 12월 31일까지이며, 2024년은 윤년이므로 날짜 차원은 366행이 된다.

`orders`에는 매출 컬럼이 없다. 매출은 S04와 같은 계산식으로 만든다.

```text
매출 = 상품 단가 × 수량 × (1 - 할인율)
```

Power BI에서는 이 계산을 측정값으로 만들고, 단가는 `products`와 맺은 관계에서 가져온다.

## 개념 설명

### 1. 관계는 엑셀 VLOOKUP을 모델로 만든 것이다

엑셀에서 `VLOOKUP`은 매 행마다 상품번호나 고객번호를 찾아 필요한 값을 복사해 온다. 이 방식은 눈으로 보기 쉽지만, 열이 늘어나고 공식이 많아질수록 관리가 어려워진다.

Power BI의 관계는 값을 복사하지 않는다. 대신 "고객번호가 같으면 이 두 표는 연결된다"는 규칙을 모델에 저장한다. 그래서 보고서에서 `customers[region]`을 축으로 놓고 `orders`의 매출을 계산하면, Power BI가 관계를 따라 주문을 지역별로 나눈다.

비유하면 S04의 VLOOKUP은 주문표 옆에 고객 사전과 상품 사전의 내용을 복사해 붙이는 방식이다. S16의 관계 모델은 주문표, 고객 사전, 상품 사전을 따로 보관하되, 사전의 어느 줄을 참조하면 되는지만 연결해 둔다.

### 2. 1:다 관계는 사전 한 줄과 거래 여러 줄의 관계다

`customers`에는 고객 한 명이 한 줄만 있어야 한다. 반면 `orders`에는 같은 고객이 여러 번 주문할 수 있으므로 같은 `customer_id`가 여러 줄 반복될 수 있다. 그래서 관계는 다음처럼 읽는다.

```text
customers[customer_id] 1 -> * orders[customer_id]
```

`products`도 같다. 상품 하나는 상품 사전에 한 번만 나오지만 주문표에서는 여러 번 팔릴 수 있다.

```text
products[product_id] 1 -> * orders[product_id]
```

입문 단계에서는 차원 테이블이 1쪽, 팩트 테이블이 다쪽이라고 기억하면 된다. 관계 방향은 차원에서 팩트로 흐르게 둔다. 즉 고객, 상품, 날짜로 주문을 필터링한다.

### 3. 스타 스키마는 분석용 표 배치도다

스타 스키마는 가운데에 팩트 테이블을 두고 주변에 차원 테이블을 배치하는 모델이다.

| 구분 | 이 차시의 표 | 설명 |
|---|---|---|
| 팩트 테이블 | `orders` | 주문이라는 사건, 수량, 할인율처럼 계산에 쓰는 값이 있다 |
| 고객 차원 | `customers` | 지역, 등급, 성별, 나이처럼 고객을 분류하는 값이 있다 |
| 상품 차원 | `products` | 상품명, 카테고리, 단가, 원가처럼 상품을 분류하는 값이 있다 |
| 날짜 차원 | `날짜테이블` | 연도, 월번호, 연월, 연월정렬처럼 시간을 분류하는 값이 있다 |

잘 만든 스타 스키마는 보고서를 만들 때 덜 헷갈린다. 필터와 축은 차원 테이블에서 가져오고, 합계와 평균은 팩트 테이블을 기준으로 계산한다. 가령 지역별 매출을 만들 때 지역은 `customers`에서 가져오고, 매출은 `orders`의 주문 건수와 `products`의 단가를 이용해 계산한다.

### 4. 날짜 차원은 시간 계산의 기준표다

`orders`에 이미 `order_date`가 있는데 왜 날짜 테이블이 또 필요할까. 주문일은 주문이 있었던 날짜만 기록한다. 반면 날짜 차원은 분석 기간의 모든 날짜를 빠짐없이 담는 기준표다.

이 차시의 날짜 차원은 2024년 1월 1일부터 2024년 12월 31일까지 하루에 한 줄씩 만든다. 이 테이블에는 `연도`, `월번호`, `연월`, `연월정렬` 파생 컬럼이 들어간다. S17에서 전월대비를 계산하려면 날짜가 연속되어 있고 날짜 컬럼이 고유해야 한다. 이 조건을 갖추도록 날짜 차원을 만들고 Power BI에서 날짜 테이블로 지정한다.

## 이 차시의 최종 모델

최종 모델은 다음 관계를 가진다.

| 1쪽 테이블 | 1쪽 컬럼 | 다쪽 테이블 | 다쪽 컬럼 | 관계 |
|---|---|---|---|---|
| `customers` | `customer_id` | `orders` | `customer_id` | 1:다 |
| `products` | `product_id` | `orders` | `product_id` | 1:다 |
| `날짜테이블` | `Date` | `orders` | `order_date` | 1:다 |

관계는 모두 활성 상태로 두고, 교차 필터 방향은 단일 방향으로 둔다. 입문 단계에서는 양방향 필터를 기본값처럼 쓰지 않는다. 숫자가 맞아 보이는 것보다 모델이 읽히는 것이 먼저다.

## 70분 수업 흐름

| 시간 | 내용 | 진행 방식 |
|---:|---|---|
| 0-7분 | S04 복습과 오늘의 문제 제시 | VLOOKUP으로 붙이던 작업을 모델 관계로 바꾼다고 설명 |
| 7-17분 | 팩트와 차원 개념 | 주문은 사건, 고객과 상품은 설명 사전이라는 비유 |
| 17-27분 | Power BI에 3표 로드 | Excel 파일에서 `orders`, `customers`, `products` 가져오기 |
| 27-38분 | 관계 만들기 | 고객-주문, 상품-주문 1:다 관계 확인 |
| 38-50분 | 날짜 차원 만들기 | `CALENDAR`로 `날짜테이블` 생성, 날짜 테이블로 지정 |
| 50-58분 | 매출 측정값과 검산 | `Total Sales` 측정값 작성, 전체 매출 카드 확인 |
| 58-66분 | 지역별 매출 분해 | `customers[region]`으로 지역별 매출 표 만들기 |
| 66-70분 | 정리와 다음 차시 연결 | S17 전월대비 DAX가 오늘 만든 날짜 차원에 기대는 구조 설명 |

## 수업의 핵심 메시지

Power BI에서 좋은 대시보드는 예쁜 차트보다 좋은 모델에서 시작한다. `orders`처럼 숫자가 쌓이는 표는 팩트로 두고, `customers`, `products`, `날짜테이블`처럼 분류와 필터를 제공하는 표는 차원으로 둔다. 표를 무조건 하나로 합치기보다 관계를 명확히 만들면, 같은 모델로 지역별, 상품별, 월별 질문에 반복해서 답할 수 있다.

## 기술 기준 참고

날짜 테이블은 고유한 날짜, 결측 없음, 연속된 날짜 범위를 만족해야 한다. 이 기준과 `CALENDAR` 함수 사용 방식은 Microsoft Learn의 Power BI 날짜 테이블 및 DAX `CALENDAR` 문서 기준과 맞춘다.

- Power BI 날짜 테이블: https://learn.microsoft.com/en-us/power-bi/transform-model/desktop-date-tables
- Power BI 스타 스키마: https://learn.microsoft.com/en-us/power-bi/guidance/star-schema
- DAX `CALENDAR`: https://learn.microsoft.com/en-us/dax/calendar-function-dax
