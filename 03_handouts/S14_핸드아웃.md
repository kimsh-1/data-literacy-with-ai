# S14 핸드아웃: 시각화 실전, 한글폰트, BI 핸드오프

## 오늘 사용할 것

- Python 3
- pandas
- matplotlib
- seaborn
- 작업 루트: `/mnt/d/ai-data-analysis-course`
- 사용 데이터:
  - `02_datasets/real_korean/seoul_subway/seoulmetro_daily_hourly_2025.csv`
  - `02_datasets/real_korean/realestate_apt/molit_apt_sale_seoul_gangnam_2024.csv`
  - `02_datasets/real_korean/prices_econ/fred_korea_cpi_all_items_monthly.csv`
  - `02_datasets/cafe_sales/cafe_sales_clean.csv`

오늘은 S13에서 배운 집계 결과를 그래프로 표현하고, BI 도구에 넘길 수 있는 CSV 파일로 저장한다. 모든 실습 코드는 한 셀씩 실행해도 되고, 하나의 `.py` 파일에 모아 실행해도 된다.

## 따라하기 1: 폴더, 패키지, 한글폰트 설정

먼저 공통 설정을 실행한다.

```python
from pathlib import Path

import matplotlib.pyplot as plt
import matplotlib.ticker as mticker
import pandas as pd
import seaborn as sns
from matplotlib import font_manager

ROOT = Path("/mnt/d/ai-data-analysis-course")
DATA = ROOT / "02_datasets"
OUT = ROOT / "06_answers/S14_outputs"
OUT.mkdir(parents=True, exist_ok=True)

font_candidates = [
    "NanumGothic",
    "NanumGothicCoding",
    "Noto Sans CJK KR",
    "Malgun Gothic",
    "AppleGothic",
    "DejaVu Sans",
]

available_fonts = {font.name for font in font_manager.fontManager.ttflist}
selected_font = next(
    (font for font in font_candidates if font in available_fonts),
    "DejaVu Sans",
)

sns.set_theme(style="whitegrid")
plt.rcParams["font.family"] = selected_font
plt.rcParams["axes.unicode_minus"] = False

print("선택된 글꼴:", selected_font)
```

한글이 네모로 보이면 나눔폰트를 설치해야 한다. Ubuntu, WSL, Colab에서는 아래 명령을 참고한다.

```bash
sudo apt-get update
sudo apt-get install -y fonts-nanum fonts-nanum-coding
```

Colab에서는 명령 앞에 `!`를 붙인다. 설치 후에도 한글이 깨지면 런타임을 다시 시작하거나 matplotlib 폰트 캐시를 지운 뒤 다시 실행한다.

## 따라하기 2: 서울 지하철 시간대 히트맵 만들기

원본 파일은 199,424행이다. 전체를 바로 그리지 않고 승차 데이터만 고른 뒤, 역별·시간대별 합계로 줄인다.

```python
subway_path = DATA / "real_korean/seoul_subway/seoulmetro_daily_hourly_2025.csv"
subway = pd.read_csv(subway_path)

time_cols = [
    "06시이전",
    "06-07시간대",
    "07-08시간대",
    "08-09시간대",
    "09-10시간대",
    "10-11시간대",
    "11-12시간대",
    "12-13시간대",
    "13-14시간대",
    "14-15시간대",
    "15-16시간대",
    "16-17시간대",
    "17-18시간대",
    "18-19시간대",
    "19-20시간대",
    "20-21시간대",
    "21-22시간대",
    "22-23시간대",
    "23-24시간대",
    "24시이후",
]

ride = subway[subway["승하차구분"] == "승차"].copy()
ride["역라벨"] = ride["호선"].astype(str) + " " + ride["역명"].astype(str)

station_hour = ride.groupby("역라벨")[time_cols].sum()
top_stations = station_hour.sum(axis=1).sort_values(ascending=False).head(10).index
heatmap_wide = station_hour.loc[top_stations, time_cols]

print(subway.shape)
print(heatmap_wide.shape)
```

확인할 결과:

```text
지하철 원본: 199,424행, 26열
히트맵 데이터: 10행, 20열
```

히트맵을 그린다.

```python
fig, ax = plt.subplots(figsize=(13, 6))
sns.heatmap(
    heatmap_wide / 1_000_000,
    cmap="Blues",
    linewidths=0.4,
    cbar_kws={"label": "연간 승차인원(백만 명)"},
    ax=ax,
)

ax.set_title("서울 지하철 주요 역의 시간대별 승차 패턴")
ax.set_xlabel("시간대")
ax.set_ylabel("역")
ax.tick_params(axis="x", labelrotation=45)

plt.tight_layout()
plt.savefig(OUT / "s14_subway_heatmap.png", dpi=160)
plt.show()
```

BI에서 쓸 수 있도록 히트맵용 데이터를 긴 표로 저장한다.

```python
heatmap_long = (
    heatmap_wide.reset_index()
    .melt(id_vars="역라벨", var_name="시간대", value_name="연간승차인원")
    .assign(연간승차인원_백만=lambda df: df["연간승차인원"] / 1_000_000)
)

heatmap_long.to_csv(
    OUT / "s14_subway_station_hour_heatmap.csv",
    index=False,
    encoding="utf-8-sig",
)

print(heatmap_long.head())
```

`heatmap_long`은 200행이다. 행 10개 역과 열 20개 시간대가 긴 표로 바뀌었기 때문이다.

## 따라하기 3: 강남 아파트 거래금액 히스토그램

아파트 실거래 데이터의 `거래금액(만원)`은 쉼표가 들어간 문자열이다. 숫자로 바꾼 뒤 억원 단위 열을 만든다.

```python
apt_path = DATA / "real_korean/realestate_apt/molit_apt_sale_seoul_gangnam_2024.csv"
apt = pd.read_csv(apt_path)

apt["거래금액_만원"] = pd.to_numeric(
    apt["거래금액(만원)"].astype(str).str.replace(",", "", regex=False),
    errors="coerce",
)
apt["거래금액_억원"] = apt["거래금액_만원"] / 10_000
apt["계약월"] = pd.to_datetime(apt["계약년월"].astype(str), format="%Y%m")

apt = apt.dropna(subset=["전용면적(㎡)", "거래금액_만원", "계약월"]).copy()

print(apt.shape)
print(apt[["전용면적(㎡)", "거래금액_만원", "거래금액_억원"]].head())
```

확인할 결과:

```text
분석 가능 행수: 3,754행
```

히스토그램을 그린다.

```python
fig, ax = plt.subplots(figsize=(9, 5))
sns.histplot(
    data=apt,
    x="거래금액_억원",
    bins=36,
    color="#A3BEFA",
    edgecolor="#2E4780",
    ax=ax,
)

ax.set_title("강남구 아파트 거래금액 분포")
ax.set_xlabel("거래금액(억원)")
ax.set_ylabel("거래건수")

plt.tight_layout()
plt.savefig(OUT / "s14_apt_price_histogram.png", dpi=160)
plt.show()
```

히스토그램은 "어느 가격대 거래가 많은가"를 보는 차트다. 정확한 단지별 비교가 아니라 전체 분포를 보는 데 적합하다.

## 따라하기 4: 면적구간별 박스플롯

전용면적을 구간으로 나눈다.

```python
apt["면적구간"] = pd.cut(
    apt["전용면적(㎡)"],
    bins=[0, 40, 60, 85, 120, float("inf")],
    labels=["40㎡ 이하", "40-60㎡", "60-85㎡", "85-120㎡", "120㎡ 초과"],
)

print(apt["면적구간"].value_counts(sort=False))
```

박스플롯을 그린다.

```python
fig, ax = plt.subplots(figsize=(10, 5))
sns.boxplot(
    data=apt,
    x="면적구간",
    y="거래금액_억원",
    color="#F5BACC",
    ax=ax,
)

ax.set_title("면적구간별 거래금액의 범위 비교")
ax.set_xlabel("면적구간")
ax.set_ylabel("거래금액(억원)")

plt.tight_layout()
plt.savefig(OUT / "s14_apt_area_boxplot.png", dpi=160)
plt.show()
```

박스플롯은 그룹별 중앙값과 퍼짐을 한 번에 보여준다. 엑셀 피벗에서 그룹별 평균만 볼 때보다 분포 차이까지 함께 읽을 수 있다.

## 따라하기 5: 산점도와 추세선

전용면적과 거래금액의 관계를 산점도와 추세선으로 본다.

```python
corr = apt["전용면적(㎡)"].corr(apt["거래금액_만원"])
print(f"상관계수 r = {corr:.3f}")

fig, ax = plt.subplots(figsize=(9, 5.5))
sns.regplot(
    data=apt,
    x="전용면적(㎡)",
    y="거래금액_억원",
    ci=None,
    scatter_kws={"s": 28, "alpha": 0.5, "color": "#F0986E"},
    line_kws={"color": "#1F2430"},
    ax=ax,
)

ax.set_title(f"전용면적과 거래금액의 관계: r={corr:.3f}")
ax.set_xlabel("전용면적(㎡)")
ax.set_ylabel("거래금액(억원)")

plt.tight_layout()
plt.savefig(OUT / "s14_apt_scatter_trend.png", dpi=160)
plt.show()
```

확인할 결과:

```text
상관계수 r = 0.806
```

상관계수가 높아도 원인을 증명한 것은 아니다. 같은 면적이라도 단지, 위치, 층, 연식, 거래 시점이 가격에 영향을 줄 수 있다.

아파트 차트에 쓴 데이터를 저장한다.

```python
apt[
    ["시군구", "단지명", "계약월", "전용면적(㎡)", "거래금액_만원", "거래금액_억원", "면적구간"]
].to_csv(
    OUT / "s14_apt_chart_ready.csv",
    index=False,
    encoding="utf-8-sig",
)
```

## 따라하기 6: FRED CPI 월별 라인차트

월별 시계열은 라인차트로 본다. 원본 767행 중 2000년 이후만 사용한다.

```python
cpi_path = DATA / "real_korean/prices_econ/fred_korea_cpi_all_items_monthly.csv"
cpi = pd.read_csv(cpi_path)

cpi["기준월"] = pd.to_datetime(cpi["observation_date"])
cpi["소비자물가지수"] = pd.to_numeric(cpi["KORCPIALLMINMEI"], errors="coerce")

cpi_recent = cpi[cpi["기준월"] >= "2000-01-01"][["기준월", "소비자물가지수"]].copy()

print(cpi.shape)
print(cpi_recent.shape)
print(cpi_recent.head())
```

확인할 결과:

```text
CPI 원본: 767행, 2열
2000년 이후: 287행
```

라인차트를 저장한다.

```python
fig, ax = plt.subplots(figsize=(10, 5))
sns.lineplot(
    data=cpi_recent,
    x="기준월",
    y="소비자물가지수",
    color="#5477C4",
    ax=ax,
)

ax.set_title("한국 소비자물가지수 월별 추세")
ax.set_xlabel("기준월")
ax.set_ylabel("소비자물가지수")

plt.tight_layout()
plt.savefig(OUT / "s14_cpi_line.png", dpi=160)
plt.show()

cpi_recent.to_csv(OUT / "s14_cpi_recent.csv", index=False, encoding="utf-8-sig")
```

## 직접 해보기: 월별 매출 추세 라인차트 저장

사용 파일:

```text
/mnt/d/ai-data-analysis-course/02_datasets/cafe_sales/cafe_sales_clean.csv
```

목표:

1. `date`를 날짜형으로 변환한다.
2. 월별 `amount` 합계를 계산한다.
3. 월별 매출 라인차트를 저장한다.
4. 월별 집계표를 CSV로 저장한다.
5. 6월이 가장 높고 2월이 가장 낮은지 확인한다.

힌트 코드:

```python
cafe = pd.read_csv(DATA / "cafe_sales/cafe_sales_clean.csv")
cafe["date"] = pd.to_datetime(cafe["date"])

monthly_sales = (
    cafe.groupby(pd.Grouper(key="date", freq="MS"))
    .agg(매출합계=("amount", "sum"), 주문수=("order_id", "count"))
    .reset_index()
    .rename(columns={"date": "월"})
)

print(monthly_sales)
```

저장할 파일 이름:

```text
/mnt/d/ai-data-analysis-course/06_answers/S14_outputs/s14_monthly_sales.csv
/mnt/d/ai-data-analysis-course/06_answers/S14_outputs/s14_monthly_sales_trend.png
```

완료 기준:

- `monthly_sales`가 6행이다.
- 2025년 6월 매출이 `16,816,500`원이다.
- 2025년 2월 매출이 `13,157,000`원이다.
- CSV 파일과 PNG 차트 파일이 실제로 저장되어 있다.

이 핸드오프(정제 결과를 CSV로 저장해 BI로 넘기는 과정)는 S19 풀사이클에서 BI 입력으로 실제 재현된다.
