# S13 핸드아웃: 그룹화·집계·재구조화

## 오늘 사용할 것

- Python 3
- pandas
- 작업 루트: `/mnt/d/ai-data-analysis-course`
- 사용 데이터:
  - `02_datasets/shop_orders/orders.csv`
  - `02_datasets/shop_orders/customers.csv`
  - `02_datasets/shop_orders/products.csv`
  - `02_datasets/real_korean/population_kosis/mois_legal_dong_gender_age_population_20260531.csv`
  - `02_datasets/real_korean/cafe_restaurant/seoul_commercial_food_stores_202109_github.csv`
  - `02_datasets/real_korean/seoul_living_pop/seoul_living_population_gu_daily.csv`

오늘은 엑셀의 VLOOKUP, 부분합, 피벗 테이블을 pandas로 옮긴다. 코드를 외우는 것보다 "어떤 표를 어떤 모양으로 바꾸는가"를 이해하는 것이 중요하다.

## 따라하기 1: 주문 3개 표 읽고 병합하기

새 파이썬 파일이나 Colab 노트북을 열고 아래 코드를 실행한다.

```python
from pathlib import Path
import pandas as pd

ROOT = Path("/mnt/d/ai-data-analysis-course")
DATA = ROOT / "02_datasets"

orders = pd.read_csv(DATA / "shop_orders/orders.csv")
customers = pd.read_csv(DATA / "shop_orders/customers.csv")
products = pd.read_csv(DATA / "shop_orders/products.csv")

print(orders.shape)
print(customers.shape)
print(products.shape)
print(orders.head())
```

확인할 결과:

```text
orders: 20,000행, 6열
customers: 3,000행, 7열
products: 200행, 5열
```

주문 표에는 `customer_id`와 `product_id`만 있고, 고객 지역과 상품 단가는 없다. 고객 표와 상품 표를 붙인다.

```python
merged = (
    orders.merge(customers, on="customer_id", how="left")
    .merge(products, on="product_id", how="left")
)

print(merged.shape)
print(merged[["region", "category", "price"]].isna().sum())
```

확인할 결과:

```text
병합 후: 20,000행, 17열
region      0
category    0
price       0
```

엑셀에서 VLOOKUP을 한 뒤 조회 실패가 있는지 확인하는 것과 같다. `how="left"`는 주문표의 20,000행을 기준으로 유지하겠다는 뜻이다.

## 따라하기 2: 매출 계산하기

`orders.csv`에는 매출 컬럼이 없다. 상품 단가와 주문 수량, 할인율로 만든다.

```python
merged["sales"] = merged["price"] * merged["qty"] * (1 - merged["discount_rate"])

print(merged[["order_id", "price", "qty", "discount_rate", "sales"]].head())
print(merged["sales"].sum())
```

확인할 결과:

```text
전체 매출 합계: 1,938,489,803
```

계산식은 아래와 같다.

```text
매출 = 단가 * 수량 * (1 - 할인율)
```

## 따라하기 3: 지역×카테고리별 다중 집계하기

지역과 카테고리를 동시에 기준으로 묶는다.

```python
region_category_summary = (
    merged.groupby(["region", "category"])
    .agg(
        주문건수=("order_id", "nunique"),
        판매수량=("qty", "sum"),
        매출합계=("sales", "sum"),
        평균주문금액=("sales", "mean"),
    )
    .reset_index()
    .sort_values("매출합계", ascending=False)
)

print(region_category_summary.head(10))
```

대표 결과:

```text
region    category  주문건수  판매수량      매출합계
서울특별시  디지털       612   658  210356387
경기도     디지털       514   565  188110354
서울특별시  패션         990  1442   86206229
경기도     패션         884  1292   75963357
```

읽는 법:

- `groupby(["region", "category"])`: 지역별로 나눈 뒤 카테고리별로 다시 나눈다.
- `주문건수=("order_id", "nunique")`: 주문번호의 고유 개수를 센다.
- `판매수량=("qty", "sum")`: 수량을 합한다.
- `매출합계=("sales", "sum")`: 매출을 합한다.
- `평균주문금액=("sales", "mean")`: 주문 한 건의 평균 매출을 구한다.

## 따라하기 4: 지역×카테고리 피벗 테이블 만들기

이번에는 엑셀 피벗처럼 지역을 행, 카테고리를 열에 놓는다.

```python
pivot = pd.pivot_table(
    merged,
    index="region",
    columns="category",
    values="sales",
    aggfunc="sum",
    fill_value=0,
    margins=True,
    margins_name="합계",
)

pivot_display = pivot.round(0).astype("int64")
print(pivot_display.head())
```

핵심 인자:

| 인자 | 뜻 | 엑셀 피벗과 비교 |
|---|---|---|
| `index` | 행 기준 | 행 영역 |
| `columns` | 열 기준 | 열 영역 |
| `values` | 계산할 값 | 값 영역 |
| `aggfunc` | 계산 방법 | 합계, 평균, 개수 |
| `fill_value` | 빈 칸 대체값 | 빈 셀에 0 표시 |

## 따라하기 5: 231열 wide 인구 데이터를 long으로 바꾸기

이번 파일은 18,623행, 231열이다. 열이 너무 많으므로 수업에서는 서울특별시 행만 먼저 골라서 `melt`한다.

```python
pop = pd.read_csv(
    DATA / "real_korean/population_kosis/mois_legal_dong_gender_age_population_20260531.csv"
)

print(pop.shape)
print(pop.columns[:15])
```

확인할 결과:

```text
(18623, 231)
```

이제 서울특별시만 남기고, `0세남자`, `1세남자` 같은 연령·성별 열을 세로로 모은다.

```python
id_cols = ["법정동코드", "기준연월", "시도명", "시군구명", "읍면동명", "리명"]

age_gender_cols = [
    col
    for col in pop.columns
    if (col.endswith("남자") or col.endswith("여자")) and col not in ["남자", "여자"]
]

seoul_pop = pop[pop["시도명"] == "서울특별시"].copy()

long_pop = seoul_pop.melt(
    id_vars=id_cols,
    value_vars=age_gender_cols,
    var_name="연령성별",
    value_name="인구수",
)

long_pop["성별"] = long_pop["연령성별"].str.extract("(남자|여자)$")
long_pop["연령"] = (
    long_pop["연령성별"].str.replace("(남자|여자)$", "", regex=True).str.strip()
)

print(seoul_pop.shape)
print(len(age_gender_cols))
print(long_pop.shape)
print(long_pop[["시도명", "시군구명", "읍면동명", "연령", "성별", "인구수"]].head())
```

확인할 결과:

```text
서울특별시 wide: 452행, 231열
melt 대상 연령/성별 열: 222개
서울특별시 long: 100,344행, 10열
```

`melt`를 하면 열 이름이 값으로 내려온다. 넓은 표에서는 `0세남자`가 열 이름이었지만, 긴 표에서는 `연령성별` 열 안의 값이 된다.

## 따라하기 6: 서울 음식점 상권 업종 상위 10개 집계하기

상가업소 파일은 119,158행이다. 원본을 그대로 보지 말고 먼저 업종별 개수로 줄인다.

```python
stores = pd.read_csv(
    DATA / "real_korean/cafe_restaurant/seoul_commercial_food_stores_202109_github.csv"
)

top10 = (
    stores.groupby("상권업종중분류명")
    .size()
    .reset_index(name="업소수")
    .sort_values("업소수", ascending=False)
    .head(10)
)

print(stores.shape)
print(top10)
```

확인할 대표 결과:

```text
상가업소 파일: 119,158행, 13열
한식 40,448
커피점/카페 19,703
유흥주점 11,693
분식 10,199
```

`groupby().size()`는 각 그룹에 행이 몇 개 있는지 세는 가장 단순한 집계다.

## 직접 해보기: 자치구별 생활인구 집계

사용 파일:

```text
02_datasets/real_korean/seoul_living_pop/seoul_living_population_gu_daily.csv
```

목표:

1. 생활인구 파일을 읽는다.
2. `기준일ID`를 날짜로 바꾼다.
3. `시군구명`이 `서울시`인 전체 합계 행은 제외한다.
4. `시군구명`별로 아래 값을 계산한다.
   - 관측일수: 날짜의 고유 개수
   - 평균생활인구: `총생활인구수` 평균
   - 평균주간인구: `주간인구수(09~18)` 평균
   - 평균야간인구: `야간인구수(19~08)` 평균
   - 최대생활인구: `일최대인구수` 최댓값
5. 평균생활인구 기준으로 내림차순 정렬한다.
6. 상위 10개 자치구를 출력한다.

시작 코드:

```python
living = pd.read_csv(
    DATA / "real_korean/seoul_living_pop/seoul_living_population_gu_daily.csv"
)

living["기준일"] = pd.to_datetime(living["기준일ID"].astype(str), format="%Y%m%d")

gu_living = living[living["시군구명"] != "서울시"].copy()

# 이 아래에 groupby 코드를 작성한다.
```

완료 확인:

```text
생활인구 파일: 77,792행, 15열
기간: 2018-04-05 ~ 2026-06-30
자치구 집계 결과: 25행
평균생활인구 1위: 강남구
강남구 관측일수: 2,992
강남구 평균생활인구: 약 846,650
```

## 자주 나는 오류

| 오류 상황 | 원인 | 해결 |
|---|---|---|
| 병합 후 행수가 20,000보다 많아짐 | 키가 중복된 표를 잘못 붙였거나 병합 기준이 잘못됨 | `on=`에 넣은 키와 오른쪽 표의 중복 여부 확인 |
| `KeyError` | 열 이름을 잘못 입력함 | `df.columns`로 실제 열 이름 확인 |
| `sales` 계산이 안 됨 | 병합 전에는 `price`가 없음 | 상품 표까지 병합한 뒤 계산 |
| `melt` 결과가 너무 커짐 | 전체 18,623행과 222개 열을 한 번에 long으로 바꿈 | 수업에서는 서울특별시만 필터링 후 실습 |
| 피벗 결과에 빈 칸이 많음 | 해당 지역×카테고리 조합이 없음 | `fill_value=0` 사용 |
