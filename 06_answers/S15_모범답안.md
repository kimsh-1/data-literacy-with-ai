# S15 모범답안: 총매출 KPI 카드

## 미션 정답

미션은 `cafe_sales_clean.csv`를 Power BI Desktop에 불러온 뒤, `amount`의 합계를 KPI 카드로 표시하는 것이다.

정답 카드 값:

```text
90,492,500
```

Power BI에서 카드가 `90.49M` 또는 `90M`처럼 보이면 계산이 틀린 것이 아니라 표시 단위가 자동 축약된 것이다. 카드 서식에서 표시 단위(Display units)를 `없음(None)`, 소수 자릿수(Decimal places)를 `0`으로 바꾸면 `90,492,500`으로 확인할 수 있다.

## 올바른 Power BI 설정

| 항목 | 설정 |
|---|---|
| 데이터 원본 | `/mnt/d/ai-data-analysis-course/02_datasets/cafe_sales/cafe_sales_clean.csv` |
| 표 이름 | `cafe_sales_clean` |
| 시각화 | 카드(Card) 또는 카드(새)(Card new) |
| 값 필드 | `amount` |
| 집계 방식 | 합계(Sum) |
| 제목 | `총매출` 또는 `총매출(원)` |
| 표시 단위 | 없음(None) |
| 소수 자릿수 | 0 |

카드에 `10,424`가 보이면 `order_id` 개수나 행 개수가 들어갔을 가능성이 높다. 카드에 약 `8,681`이 보이면 `amount` 평균이 들어갔을 가능성이 높다. 미션의 기준은 `amount` 합계이다.

## 함께 완성되어야 하는 첫 페이지

첫 페이지에는 다음 세 시각화가 있으면 된다.

| 시각화 | 필드 배치 | 목적 |
|---|---|---|
| 총매출 KPI 카드 | `amount` 합계 | 핵심 숫자 확인 |
| 카테고리별 매출 막대그래프 | X축 `category`, Y축 `amount` 합계 | 상품군별 매출 비교 |
| 일자별 매출 꺾은선그래프 | X축 `date`, Y축 `amount` 합계 | 기간 흐름 확인 |

카테고리별 매출 검산값:

| category | amount 합계 |
|---|---:|
| 커피 | 45,606,000 |
| 음료 | 25,550,000 |
| 디저트 | 19,336,500 |

월별 매출 참고값:

| 월 | amount 합계 |
|---|---:|
| 2025-01 | 14,426,500 |
| 2025-02 | 13,157,000 |
| 2025-03 | 14,735,500 |
| 2025-04 | 15,263,000 |
| 2025-05 | 16,094,000 |
| 2025-06 | 16,816,500 |

## 검증용 Python 코드

S15는 Power BI 차시이므로 별도 `S15_code.py` 파일을 요구하지 않는다. 아래 코드는 모범답안 수치 검증을 위해 `python3`와 `pandas 3.0.3`으로 실제 실행한 코드이다.

```python
from pathlib import Path
import pandas as pd

path = Path("/mnt/d/ai-data-analysis-course/02_datasets/cafe_sales/cafe_sales_clean.csv")
df = pd.read_csv(path)

print("shape:", df.shape)
print("date_min:", df["date"].min())
print("date_max:", df["date"].max())
print("total_amount:", int(df["amount"].sum()))
print("order_count:", len(df))
print("null_total:", int(df.isna().sum().sum()))

category_summary = (
    df.groupby("category", as_index=False)["amount"]
    .sum()
    .sort_values("amount", ascending=False)
)
print(category_summary.to_string(index=False))

df["date_dt"] = pd.to_datetime(df["date"])
monthly = df.groupby(df["date_dt"].dt.to_period("M"))["amount"].sum().reset_index()
monthly["date_dt"] = monthly["date_dt"].astype(str)
print(monthly.to_string(index=False))
```

실행 확인 결과:

```text
shape: (10424, 12)
date_min: 2025-01-01
date_max: 2025-06-30
total_amount: 90492500
order_count: 10424
null_total: 0

category   amount
      커피 45606000
      음료 25550000
     디저트 19336500

date_dt   amount
2025-01 14426500
2025-02 13157000
2025-03 14735500
2025-04 15263000
2025-05 16094000
2025-06 16816500
```

## 제출 예시 설명

제출 화면을 말로 설명하면 다음과 같다.

상단에는 `총매출(원)` 카드가 있고 값은 `90,492,500`이다. 왼쪽에는 `카테고리별 매출` 막대그래프가 있으며 커피가 가장 높고, 음료, 디저트 순서로 보인다. 오른쪽 또는 아래에는 `일자별 매출` 꺾은선그래프가 있으며 2025년 1월 1일부터 6월 30일까지의 매출 흐름이 보인다.

## 흔한 오답과 수정

| 오답 상태 | 원인 | 수정 |
|---|---|---|
| 카드 값이 `10,424` | 주문 건수 또는 행 개수를 표시함 | `amount`를 값에 넣고 합계로 바꾼다 |
| 카드 값이 약 `8,681` | `amount` 평균을 표시함 | 집계 방식을 합계로 바꾼다 |
| 카드 값이 `90.49M` | 표시 단위가 자동 축약됨 | 표시 단위를 없음으로 바꾼다 |
| 막대그래프 값이 카테고리 개수처럼 보임 | `amount`가 아니라 `category` 개수가 값에 들어감 | Y축 또는 값에 `amount` 합계를 넣는다 |
| 꺾은선그래프가 연도 한 점만 보임 | 날짜 계층 구조의 연도 수준만 표시됨 | X축에서 `date` 자체를 선택하거나 날짜 계층을 일 수준으로 펼친다 |

## 완료 기준

총매출 카드가 `90,492,500`으로 검산되고, 같은 페이지에 카테고리별 매출 막대그래프와 일자별 매출 꺾은선그래프가 함께 있으면 미션 완료이다.
