# S04 모범답안: 등급별 객단가

## 미션

고객 등급별로 주문 건수, 매출 합계, 객단가를 구한다.

객단가는 이 실습에서 주문 1건당 평균 매출을 뜻한다.

```text
객단가 = 매출 합계 ÷ 주문 건수
```

## 전제

`orders` 시트에 따라하기 결과가 이미 만들어져 있어야 한다.

| 열 | 내용 |
|---|---|
| I열 | `price` |
| J열 | `region` |
| K열 | `grade` |
| L열 | `sales` |

주문별 매출 공식은 다음과 같다.

```excel
=I2*E2*(1-F2)
```

## 엑셀 풀이

새 시트 `grade_summary`를 만들고 A1:D1에 다음 제목을 입력한다.

| 셀 | 제목 |
|---|---|
| A1 | `grade` |
| B1 | `order_count` |
| C1 | `total_sales` |
| D1 | `avg_order_value` |

A2:A5에는 등급을 입력한다.

```text
VIP
골드
실버
브론즈
```

B2 셀:

```excel
=COUNTIF(orders!$K$2:$K$20001,A2)
```

C2 셀:

```excel
=SUMIF(orders!$K$2:$K$20001,A2,orders!$L$2:$L$20001)
```

D2 셀:

```excel
=C2/B2
```

B2:D2를 5행까지 채운다.

## 정답

| grade | order_count | total_sales | avg_order_value |
|---|---:|---:|---:|
| VIP | 3,757 | 393,432,930 | 104,719.97 |
| 골드 | 5,433 | 547,524,985 | 100,777.65 |
| 실버 | 6,373 | 603,583,519 | 94,709.48 |
| 브론즈 | 4,437 | 393,948,369 | 88,787.10 |

해석: 네 등급 중 주문 1건당 평균 매출이 가장 높은 등급은 VIP다. VIP의 객단가는 104,719.97원이고, 브론즈의 객단가 88,787.10원보다 높다.

## 검산값

| 항목 | 값 |
|---|---:|
| 전체 주문 건수 | 20,000 |
| 전체 매출 | 1,938,489,803 |
| 전체 객단가 | 96,924.49 |

등급별 주문 건수 합계와 매출 합계가 위 검산값과 일치해야 한다.

## 강사용 검증 코드

아래 코드는 엑셀 풀이와 같은 계산을 pandas로 재현해 검산한 코드다. 이 차시는 엑셀 함수 차시이므로 수강생 실습용 코드는 엑셀 공식이며, 별도 `S04_code.py` 산출물은 만들지 않는다.

```python
import pandas as pd
from pathlib import Path

base = Path("/mnt/d/ai-data-analysis-course/02_datasets/shop_orders")

orders = pd.read_csv(base / "orders.csv")
customers = pd.read_csv(base / "customers.csv")
products = pd.read_csv(base / "products.csv")

merged = orders.merge(
    products[["product_id", "price"]],
    on="product_id",
    how="left",
)
merged = merged.merge(
    customers[["customer_id", "grade"]],
    on="customer_id",
    how="left",
)

merged["sales"] = merged["price"] * merged["qty"] * (1 - merged["discount_rate"])

answer = (
    merged.groupby("grade", as_index=False)
    .agg(
        order_count=("order_id", "count"),
        total_sales=("sales", "sum"),
        avg_order_value=("sales", "mean"),
    )
    .sort_values("avg_order_value", ascending=False)
)

print(answer.to_string(index=False, float_format=lambda x: f"{x:.2f}"))
print("orders", len(merged))
print("total_sales", f"{merged['sales'].sum():.2f}")
```

실행 확인 결과:

```text
grade  order_count  total_sales  avg_order_value
  VIP         3757 393432930.00        104719.97
   골드         5433 547524985.00        100777.65
   실버         6373 603583519.00         94709.48
  브론즈         4437 393948369.00         88787.10
orders 20000
total_sales 1938489803.00
```
