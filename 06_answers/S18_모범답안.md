# S18 모범답안: Looker Studio 공유 링크 생성과 검증

## 미션 정답의 형태

이번 미션은 정해진 하나의 파일을 제출하는 문제가 아니라, Looker Studio 보고서 링크를 만들고 실제로 열리는지 검증하는 문제이다. 정답은 다음 조건을 만족해야 한다.

| 항목 | 정답 기준 |
|---|---|
| 데이터 원본 | `/mnt/d/ai-data-analysis-course/02_datasets/survey_responses/survey_responses_clean.csv` |
| 보고서 제목 | `S18_설문_공유_대시보드_본인이름` |
| 공유 권한 | Viewer, 보기 가능 |
| 링크 검증 | 시크릿 창, 동료 계정, 강사 계정 중 하나로 열림 확인 |
| 응답 수 | 1,500 |
| 평균 NPS | 약 6.86 |
| 평균 만족도 | 약 3.62 |
| NPS 점수 | 약 -4.73 |

공유 링크는 수강생 계정마다 다르므로 모범답안에 고정 URL은 없다. 링크가 열리고, 보기 권한이며, 핵심 숫자가 검산값과 맞으면 정답으로 본다.

## Looker Studio 계산 필드 정답

### `NPS_그룹`

```text
CASE
  WHEN nps >= 9 THEN "추천 고객"
  WHEN nps >= 7 THEN "중립 고객"
  ELSE "비추천 고객"
END
```

이 필드는 NPS 원점수를 세 그룹으로 나눈다.

| NPS 그룹 | 응답 수 |
|---|---:|
| 비추천 고객 | 552 |
| 추천 고객 | 481 |
| 중립 고객 | 467 |

### `NPS_점수_행`

```text
CASE
  WHEN nps >= 9 THEN 100
  WHEN nps <= 6 THEN -100
  ELSE 0
END
```

이 필드는 반드시 평균으로 집계한다. 평균값이 NPS 점수이며, 정답은 약 `-4.73`이다.

### `만족도_구간`

선택 필드이다.

```text
CASE
  WHEN satisfaction >= 4 THEN "만족"
  WHEN satisfaction = 3 THEN "보통"
  ELSE "불만족"
END
```

## 완성 대시보드의 권장 구성

| 시각화 | 차원 | 측정항목 | 정답 확인 |
|---|---|---|---|
| 응답 수 카드 | 없음 | `respondent_id` 개수 또는 고유 개수 | 1,500 |
| 평균 NPS 카드 | 없음 | `nps` 평균 | 6.86 |
| 평균 만족도 카드 | 없음 | `satisfaction` 평균 | 3.62 |
| NPS 점수 카드 | 없음 | `NPS_점수_행` 평균 | -4.73 |
| NPS 그룹별 막대그래프 | `NPS_그룹` | 응답 수 | 552, 481, 467 |
| 채널별 표 | `channel` | 응답 수, 평균 NPS, 평균 만족도 | 네이버 검색 435건 |
| 연령대별 막대그래프 | `age_group` | 평균 만족도 | 40대와 50대가 상대적으로 높음 |

## 채널별 검산값

| channel | 응답 수 | 평균 NPS | 평균 만족도 |
|---|---:|---:|---:|
| 네이버 검색 | 435 | 6.87 | 3.62 |
| 카카오톡 채널 | 259 | 6.93 | 3.61 |
| 인스타그램 | 251 | 6.94 | 3.67 |
| 지인 추천 | 172 | 6.44 | 3.47 |
| 오프라인 매장 | 151 | 6.48 | 3.55 |
| 이메일 | 117 | 7.11 | 3.71 |
| 앱 푸시 | 115 | 7.30 | 3.81 |

채널별 표가 이 값과 소수점 반올림 수준에서 맞으면 정상이다.

## 연령대별 검산값

| age_group | 응답 수 | 평균 NPS | 평균 만족도 |
|---|---:|---:|---:|
| 30대 | 406 | 6.80 | 3.63 |
| 40대 | 344 | 7.14 | 3.71 |
| 20대 | 335 | 6.69 | 3.54 |
| 50대 | 249 | 7.04 | 3.70 |
| 60대 이상 | 92 | 6.70 | 3.57 |
| 10대 | 74 | 6.18 | 3.43 |

이 표는 원인 분석이 아니라 검산용이다. 예를 들어 10대의 평균 만족도가 낮다고 해서 곧바로 "10대가 불만족한 원인"을 단정하지 않는다.

## 공유 링크 검증 체크리스트

| 확인 항목 | 통과 기준 |
|---|---|
| 링크가 열린다 | 다른 브라우저, 시크릿 창, 동료 계정 중 하나에서 열림 |
| 권한이 적절하다 | Viewer 권한이며 편집 화면이 아님 |
| 핵심 카드가 보인다 | 응답 수, 평균 NPS, 평균 만족도, NPS 점수 카드가 보임 |
| 숫자가 맞다 | 1,500, 6.86, 3.62, -4.73 근처 |
| 민감 정보가 없다 | 수업용 합성 데이터만 사용 |

## 흔한 오답과 수정

| 오답 상태 | 원인 | 수정 |
|---|---|---|
| 응답 수가 3,000 이상 | 같은 CSV를 중복 업로드함 | 새 데이터 원본으로 다시 연결하거나 중복 데이터셋을 정리한다 |
| 평균 NPS가 매우 큰 값 | `nps`가 합계로 집계됨 | 집계를 평균으로 바꾼다 |
| NPS 점수가 -7,100처럼 보임 | `NPS_점수_행`이 합계로 집계됨 | 집계를 평균으로 바꾼다 |
| 링크를 받은 사람이 못 봄 | 보고서 권한이 닫혀 있음 | Share 설정에서 Viewer 권한을 부여한다 |
| 링크를 받은 사람이 편집 가능 | Editor 권한으로 공유함 | Viewer 권한으로 낮춘다 |
| 한글이 깨짐 | 다른 인코딩 파일을 올림 | 수업 배포본 UTF-8 CSV를 다시 업로드한다 |

## 검증용 Python 코드

S18은 Looker Studio 공유 차시이므로 별도 `S18_code.py` 파일을 요구하지 않는다. 아래 코드는 모범답안 수치 검증을 위해 `python3`와 `pandas 3.0.3`으로 실제 실행한 코드이다.

```python
from pathlib import Path
import pandas as pd

path = Path("/mnt/d/ai-data-analysis-course/02_datasets/survey_responses/survey_responses_clean.csv")
df = pd.read_csv(path, parse_dates=["submitted_at"])

print("shape:", df.shape)
print("columns:", list(df.columns))
print("submitted_min:", df["submitted_at"].min())
print("submitted_max:", df["submitted_at"].max())
print("unique_respondents:", df["respondent_id"].nunique())
print("missing_total:", int(df.isna().sum().sum()))

promoters = (df["nps"] >= 9).sum()
passives = df["nps"].between(7, 8).sum()
detractors = (df["nps"] <= 6).sum()
nps_score = ((promoters - detractors) / len(df)) * 100

print("avg_nps:", round(df["nps"].mean(), 4))
print("avg_satisfaction:", round(df["satisfaction"].mean(), 4))
print("promoters:", int(promoters))
print("passives:", int(passives))
print("detractors:", int(detractors))
print("nps_score:", round(nps_score, 2))

channel_summary = (
    df.groupby("channel")
    .agg(
        responses=("respondent_id", "count"),
        avg_nps=("nps", "mean"),
        avg_satisfaction=("satisfaction", "mean"),
    )
    .sort_values("responses", ascending=False)
)
print(channel_summary.round(2).to_string())

age_summary = (
    df.groupby("age_group")
    .agg(
        responses=("respondent_id", "count"),
        avg_nps=("nps", "mean"),
        avg_satisfaction=("satisfaction", "mean"),
    )
    .sort_values("responses", ascending=False)
)
print(age_summary.round(2).to_string())
```

실행 확인 결과:

```text
shape: (1500, 8)
columns: ['respondent_id', 'age_group', 'gender', 'nps', 'satisfaction', 'channel', 'free_text', 'submitted_at']
submitted_min: 2025-03-01 09:35:00
submitted_max: 2025-05-31 21:26:00
unique_respondents: 1500
missing_total: 0
avg_nps: 6.8553
avg_satisfaction: 3.624
promoters: 481
passives: 467
detractors: 552
nps_score: -4.73

channel      responses  avg_nps  avg_satisfaction
네이버 검색            435     6.87              3.62
카카오톡 채널           259     6.93              3.61
인스타그램             251     6.94              3.67
지인 추천             172     6.44              3.47
오프라인 매장           151     6.48              3.55
이메일               117     7.11              3.71
앱 푸시              115     7.30              3.81

age_group  responses  avg_nps  avg_satisfaction
30대              406     6.80              3.63
40대              344     7.14              3.71
20대              335     6.69              3.54
50대              249     7.04              3.70
60대 이상            92     6.70              3.57
10대               74     6.18              3.43
```

## 완료 기준

공유 링크가 Viewer 권한으로 열리고, 대시보드의 핵심 숫자가 응답 수 `1,500`, 평균 NPS 약 `6.86`, 평균 만족도 약 `3.62`, NPS 점수 약 `-4.73`으로 검산되면 미션 완료이다.
